# Creator Proof - Flutter Mobile App

## 🚀 Quick Start

### Prerequisites

1. **Flutter SDK** - Install from https://flutter.dev/docs/get-started/install
2. **Android Studio** - For Android development
3. **Firebase Project** - With Android app configured

### Setup Steps

#### Step 1: Install Flutter

1. Download Flutter from https://flutter.dev
2. Extract to a folder (e.g., `C:\flutter`)
3. Add Flutter to PATH:
   - Search "Environment Variables" in Windows
   - Edit PATH, add `C:\flutter\bin`
4. Open new terminal, run: `flutter doctor`

#### Step 2: Configure Firebase

1. Go to Firebase Console → Project Settings
2. Click "Add app" → Android
3. Package name: `com.example.creator_proof` (or your package name)
4. Download `google-services.json`
5. Place it in `android/app/` folder

#### Step 3: Install Dependencies

```bash
cd app.cep/cep_app
flutter pub get
```

#### Step 4: Run the App

```bash
# Connect Android device or start emulator
flutter run
```

### Project Structure

```
lib/
├── main.dart              # App entry point
├── models/                # Data models
│   ├── user_model.dart
│   └── proof_model.dart
├── services/              # Backend services
│   ├── auth_service.dart      # Google Sign-In
│   ├── wallet_service.dart    # WalletConnect
│   ├── proof_service.dart     # Proof CRUD
│   ├── hash_service.dart      # SHA-256
│   └── blockchain_service.dart # Polygon
├── screens/               # App screens
│   ├── splash_screen.dart
│   ├── login_screen.dart
│   ├── home_screen.dart
│   ├── upload_screen.dart
│   ├── verify_screen.dart
│   ├── explore_screen.dart
│   └── profile_screen.dart
└── widgets/               # Reusable widgets
    └── proof_card.dart
```

### Features

- ✅ Google Sign-In authentication
- ✅ File upload with SHA-256 hashing
- ✅ Blockchain proof storage (Polygon)
- ✅ WalletConnect integration
- ✅ Proof verification
- ✅ Public proof exploration
- ✅ User profile management

### Environment Setup

Create `lib/firebase_options.dart` with your Firebase config:

```dart
// Generated by FlutterFire CLI
// Run: flutterfire configure
```

Or manually add Firebase:

1. Add `google-services.json` to `android/app/`
2. Update `android/build.gradle` with Google services plugin
3. Update `android/app/build.gradle` with Firebase dependencies

### Building for Release

```bash
# Build APK
flutter build apk --release

# Build App Bundle (for Play Store)
flutter build appbundle --release
```

### Troubleshooting

**"Flutter not found"**

- Make sure Flutter is in your PATH
- Restart terminal after adding to PATH

**Firebase errors**

- Verify `google-services.json` is in correct location
- Check package name matches Firebase app

**WalletConnect not connecting**

- Get a project ID from https://cloud.walletconnect.com
- Update `wallet_service.dart` with your project ID
